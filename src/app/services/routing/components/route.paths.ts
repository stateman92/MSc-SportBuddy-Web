export enum RoutePaths {
  login = 'login',
  upload = 'upload',
  uploadWithParam = 'upload/:id',
  commands = 'commands',
  users = 'users',
  exercises = 'exercises',
  resetPassword = 'resetPassword',
  default = '**'
}
